bootJar { enabled = false }
jar { enabled = true }

dependencies {
    // Spring Data JPA
    // gradle 4 이하 compile -> implementation / api
    api group: 'org.springframework.boot', name: 'spring-boot-starter-data-jpa', version: '2.2.2.RELEASE'
    // H2
    runtimeOnly 'com.h2database:h2'
}